	struct ID
		String id

	contract Init
		optional down onready
		optional down update (Any obj)
	
	contract Credentials
		up logout
	
	contract KeyValue
		up   save (Any obj)

	contract KVUpdate
		down update (Any obj)
		
	contract Query
		up	 scan  (String resource) (String type) (QueryHandler handler)
	
// I wonder if the key here should be a Crokey
	contract QueryHandler
		down entry  (String key) (Any value)