	cross Nil = Nil
	cross [h] = [h]
	cross (Cons { head: h, tail: t }) = map (merge (cross t)) h
//	cross (Cons { head: h, tail: t }) = map (merge Nil) h
	merge [] a = [[a]]
	merge (Cons[List[A]] l) s = map (Cons s) l

//		merge Nil k = []
//	inner t = cross t
