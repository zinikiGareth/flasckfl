	test that the card initially does not have a show tag
		create s ControlledCard
		matchClass div>span
		
	test that the card has a show tag after sending
		create s ControlledCard
		send s SetState setOn
		matchClass div>span
			show
			
	test that the card can echo a simple string
		create s ControlledCard
		send s Echo saySomething "hello"
			Echo echoIt "hello"