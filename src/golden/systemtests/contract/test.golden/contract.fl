A contract to enable us to be invoked

	contract HitMe
		hit

A card that stores a value.
The value is multiplied by 10 every time hit is invoked

	card Multiplier
		state
			Number value <- 42
			
		implements HitMe
			hit
				value <- value * 10