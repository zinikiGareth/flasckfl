	object AssignList
		state
			List[Number] ns <- [ 10, 20 ]
		
		ctor create
		
		method update1 (Number n)
			first <- n
			second <- n/2
				first = item 0 ns
				second = item 1 ns

		acor hd = (item 0 ns).head